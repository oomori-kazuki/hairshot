<header class="mb-4">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <div class="header_icon">
      <% unless controller.controller_name == 'ToppagesController' %>
        <% if logged_in? %>
          <%= image_tag current_user.image.thumb.url, class: "rounded-circle" %>
        <% end %>
      <% end %>
    </div>
    <a class="navbar-brand " href="/">hairshot</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
      <ul class="navbar-nav">
         <% if logged_in? %>
          <li class="nav-item"><%= link_to 'Top', root_path, class: "nav-link" %></li>
          <li class="nav-item">
            <%= link_to new_post_path do %>
              <i class="fa fa-edit"></i>
            <% end %>
          </li>
          <li class="nav-item"><%= link_to "Home", posts_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "My page", user_path(current_user), class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Users", users_path(@users), class: "nav-link" %></li>
          <li class="nav-item"><%= link_to 'Logout', logout_path, method: :delete, class: "nav-link" %></li>
        <% else %>
          <li class="nav-item"><%= link_to "Signup", signup_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Login", login_path, class: "nav-link" %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>